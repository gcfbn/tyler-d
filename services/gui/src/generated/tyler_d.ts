// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v3.21.12
// source: tyler_d.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { CallContext, CallOptions } from "nice-grpc-common";

export const protobufPackage = "tyler_d";

export interface ListModelsRequest {
}

export interface ListModelsResponse {
  models: ModelInfo[];
}

export interface ModelInfo {
  id: string;
  /** e.g., "ollama", "openai", "gemini" */
  provider: string;
}

export interface IngestRequest {
  text?: string | undefined;
  file?: FileContent | undefined;
}

export interface FileContent {
  content: Uint8Array;
  /** e.g., "pdf", "png" */
  fileType: string;
  fileName: string;
}

export interface IngestResponse {
  success: boolean;
  message: string;
}

export interface Message {
  /** e.g., "user", "assistant" */
  role: string;
  content: string;
}

export interface AskRequest {
  query: string;
  /** UI passes the context here */
  history: Message[];
}

export interface AskResponse {
  answer: string;
}

function createBaseListModelsRequest(): ListModelsRequest {
  return {};
}

export const ListModelsRequest: MessageFns<ListModelsRequest> = {
  encode(_: ListModelsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListModelsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListModelsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): ListModelsRequest {
    return {};
  },

  toJSON(_: ListModelsRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<ListModelsRequest>): ListModelsRequest {
    return ListModelsRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<ListModelsRequest>): ListModelsRequest {
    const message = createBaseListModelsRequest();
    return message;
  },
};

function createBaseListModelsResponse(): ListModelsResponse {
  return { models: [] };
}

export const ListModelsResponse: MessageFns<ListModelsResponse> = {
  encode(message: ListModelsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.models) {
      ModelInfo.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListModelsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListModelsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.models.push(ModelInfo.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListModelsResponse {
    return {
      models: globalThis.Array.isArray(object?.models) ? object.models.map((e: any) => ModelInfo.fromJSON(e)) : [],
    };
  },

  toJSON(message: ListModelsResponse): unknown {
    const obj: any = {};
    if (message.models?.length) {
      obj.models = message.models.map((e) => ModelInfo.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<ListModelsResponse>): ListModelsResponse {
    return ListModelsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListModelsResponse>): ListModelsResponse {
    const message = createBaseListModelsResponse();
    message.models = object.models?.map((e) => ModelInfo.fromPartial(e)) || [];
    return message;
  },
};

function createBaseModelInfo(): ModelInfo {
  return { id: "", provider: "" };
}

export const ModelInfo: MessageFns<ModelInfo> = {
  encode(message: ModelInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.provider !== "") {
      writer.uint32(18).string(message.provider);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ModelInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModelInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.provider = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModelInfo {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      provider: isSet(object.provider) ? globalThis.String(object.provider) : "",
    };
  },

  toJSON(message: ModelInfo): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.provider !== "") {
      obj.provider = message.provider;
    }
    return obj;
  },

  create(base?: DeepPartial<ModelInfo>): ModelInfo {
    return ModelInfo.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ModelInfo>): ModelInfo {
    const message = createBaseModelInfo();
    message.id = object.id ?? "";
    message.provider = object.provider ?? "";
    return message;
  },
};

function createBaseIngestRequest(): IngestRequest {
  return { text: undefined, file: undefined };
}

export const IngestRequest: MessageFns<IngestRequest> = {
  encode(message: IngestRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.text !== undefined) {
      writer.uint32(10).string(message.text);
    }
    if (message.file !== undefined) {
      FileContent.encode(message.file, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IngestRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIngestRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.text = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.file = FileContent.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IngestRequest {
    return {
      text: isSet(object.text) ? globalThis.String(object.text) : undefined,
      file: isSet(object.file) ? FileContent.fromJSON(object.file) : undefined,
    };
  },

  toJSON(message: IngestRequest): unknown {
    const obj: any = {};
    if (message.text !== undefined) {
      obj.text = message.text;
    }
    if (message.file !== undefined) {
      obj.file = FileContent.toJSON(message.file);
    }
    return obj;
  },

  create(base?: DeepPartial<IngestRequest>): IngestRequest {
    return IngestRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<IngestRequest>): IngestRequest {
    const message = createBaseIngestRequest();
    message.text = object.text ?? undefined;
    message.file = (object.file !== undefined && object.file !== null)
      ? FileContent.fromPartial(object.file)
      : undefined;
    return message;
  },
};

function createBaseFileContent(): FileContent {
  return { content: new Uint8Array(0), fileType: "", fileName: "" };
}

export const FileContent: MessageFns<FileContent> = {
  encode(message: FileContent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.content.length !== 0) {
      writer.uint32(10).bytes(message.content);
    }
    if (message.fileType !== "") {
      writer.uint32(18).string(message.fileType);
    }
    if (message.fileName !== "") {
      writer.uint32(26).string(message.fileName);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FileContent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFileContent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.content = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.fileType = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.fileName = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FileContent {
    return {
      content: isSet(object.content) ? bytesFromBase64(object.content) : new Uint8Array(0),
      fileType: isSet(object.fileType)
        ? globalThis.String(object.fileType)
        : isSet(object.file_type)
        ? globalThis.String(object.file_type)
        : "",
      fileName: isSet(object.fileName)
        ? globalThis.String(object.fileName)
        : isSet(object.file_name)
        ? globalThis.String(object.file_name)
        : "",
    };
  },

  toJSON(message: FileContent): unknown {
    const obj: any = {};
    if (message.content.length !== 0) {
      obj.content = base64FromBytes(message.content);
    }
    if (message.fileType !== "") {
      obj.fileType = message.fileType;
    }
    if (message.fileName !== "") {
      obj.fileName = message.fileName;
    }
    return obj;
  },

  create(base?: DeepPartial<FileContent>): FileContent {
    return FileContent.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<FileContent>): FileContent {
    const message = createBaseFileContent();
    message.content = object.content ?? new Uint8Array(0);
    message.fileType = object.fileType ?? "";
    message.fileName = object.fileName ?? "";
    return message;
  },
};

function createBaseIngestResponse(): IngestResponse {
  return { success: false, message: "" };
}

export const IngestResponse: MessageFns<IngestResponse> = {
  encode(message: IngestResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IngestResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIngestResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IngestResponse {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
    };
  },

  toJSON(message: IngestResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create(base?: DeepPartial<IngestResponse>): IngestResponse {
    return IngestResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<IngestResponse>): IngestResponse {
    const message = createBaseIngestResponse();
    message.success = object.success ?? false;
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseMessage(): Message {
  return { role: "", content: "" };
}

export const Message: MessageFns<Message> = {
  encode(message: Message, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.role !== "") {
      writer.uint32(10).string(message.role);
    }
    if (message.content !== "") {
      writer.uint32(18).string(message.content);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Message {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.role = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.content = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Message {
    return {
      role: isSet(object.role) ? globalThis.String(object.role) : "",
      content: isSet(object.content) ? globalThis.String(object.content) : "",
    };
  },

  toJSON(message: Message): unknown {
    const obj: any = {};
    if (message.role !== "") {
      obj.role = message.role;
    }
    if (message.content !== "") {
      obj.content = message.content;
    }
    return obj;
  },

  create(base?: DeepPartial<Message>): Message {
    return Message.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Message>): Message {
    const message = createBaseMessage();
    message.role = object.role ?? "";
    message.content = object.content ?? "";
    return message;
  },
};

function createBaseAskRequest(): AskRequest {
  return { query: "", history: [] };
}

export const AskRequest: MessageFns<AskRequest> = {
  encode(message: AskRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.query !== "") {
      writer.uint32(10).string(message.query);
    }
    for (const v of message.history) {
      Message.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AskRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAskRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.query = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.history.push(Message.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AskRequest {
    return {
      query: isSet(object.query) ? globalThis.String(object.query) : "",
      history: globalThis.Array.isArray(object?.history) ? object.history.map((e: any) => Message.fromJSON(e)) : [],
    };
  },

  toJSON(message: AskRequest): unknown {
    const obj: any = {};
    if (message.query !== "") {
      obj.query = message.query;
    }
    if (message.history?.length) {
      obj.history = message.history.map((e) => Message.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<AskRequest>): AskRequest {
    return AskRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AskRequest>): AskRequest {
    const message = createBaseAskRequest();
    message.query = object.query ?? "";
    message.history = object.history?.map((e) => Message.fromPartial(e)) || [];
    return message;
  },
};

function createBaseAskResponse(): AskResponse {
  return { answer: "" };
}

export const AskResponse: MessageFns<AskResponse> = {
  encode(message: AskResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.answer !== "") {
      writer.uint32(10).string(message.answer);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AskResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAskResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.answer = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AskResponse {
    return { answer: isSet(object.answer) ? globalThis.String(object.answer) : "" };
  },

  toJSON(message: AskResponse): unknown {
    const obj: any = {};
    if (message.answer !== "") {
      obj.answer = message.answer;
    }
    return obj;
  },

  create(base?: DeepPartial<AskResponse>): AskResponse {
    return AskResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AskResponse>): AskResponse {
    const message = createBaseAskResponse();
    message.answer = object.answer ?? "";
    return message;
  },
};

export type OrchestratorDefinition = typeof OrchestratorDefinition;
export const OrchestratorDefinition = {
  name: "Orchestrator",
  fullName: "tyler_d.Orchestrator",
  methods: {
    /** Ingest a piece of text or a file */
    ingest: {
      name: "Ingest",
      requestType: IngestRequest as typeof IngestRequest,
      requestStream: false,
      responseType: IngestResponse as typeof IngestResponse,
      responseStream: false,
      options: {},
    },
    /** Ask a question based on stored knowledge */
    ask: {
      name: "Ask",
      requestType: AskRequest as typeof AskRequest,
      requestStream: false,
      responseType: AskResponse as typeof AskResponse,
      responseStream: false,
      options: {},
    },
    /** List available models from the provider */
    listModels: {
      name: "ListModels",
      requestType: ListModelsRequest as typeof ListModelsRequest,
      requestStream: false,
      responseType: ListModelsResponse as typeof ListModelsResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

export interface OrchestratorServiceImplementation<CallContextExt = {}> {
  /** Ingest a piece of text or a file */
  ingest(request: IngestRequest, context: CallContext & CallContextExt): Promise<DeepPartial<IngestResponse>>;
  /** Ask a question based on stored knowledge */
  ask(request: AskRequest, context: CallContext & CallContextExt): Promise<DeepPartial<AskResponse>>;
  /** List available models from the provider */
  listModels(
    request: ListModelsRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<ListModelsResponse>>;
}

export interface OrchestratorClient<CallOptionsExt = {}> {
  /** Ingest a piece of text or a file */
  ingest(request: DeepPartial<IngestRequest>, options?: CallOptions & CallOptionsExt): Promise<IngestResponse>;
  /** Ask a question based on stored knowledge */
  ask(request: DeepPartial<AskRequest>, options?: CallOptions & CallOptionsExt): Promise<AskResponse>;
  /** List available models from the provider */
  listModels(
    request: DeepPartial<ListModelsRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<ListModelsResponse>;
}

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
